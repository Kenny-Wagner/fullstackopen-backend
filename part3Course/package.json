{
  "name": "backend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "tunnel": "lt --port 3001",
    "build:ui": "rm -rf dist && cd ../../fullstackopen/part2/preexercise && npm run build && cp -r dist ../../../fullstackopen-backend/part3Course",
    "deploy:full": " npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "Kenny Wagner",
  "license": "MIT",
  "dependencies": {
    "0g": "^0.4.2",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "mongoose": "^8.4.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
